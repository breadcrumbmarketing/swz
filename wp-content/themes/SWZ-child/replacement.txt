/* Slider Container */
.recently-viewed-slider {
    margin: 20px 0; /* Adds vertical margin for spacing */
}

/* Slider Slide Element */
.recently-viewed-slider .slide {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: start;
    background: white;
    padding: 15px;
    margin: 0 10px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    border-radius: 10px;
    text-align: center;
    overflow: hidden;
    height: auto; /* Adjust if necessary to accommodate all content */
}

.recently-viewed-slider .slide .image-container {
    height: 180px; /* Set a fixed height for the image area */
    width: 100%; /* Ensure the container takes up the full width of the slide */
    background-position: center; /* Center the background image */
    background-size: cover; /* Cover the area of the container, cropping as needed */
    background-repeat: no-repeat; /* Do not repeat the background image */
    margin-bottom: 15px; /* Space between the image and the text below it */
}


/* Responsive Settings */
@media (max-width: 1024px) {
    .recently-viewed-slider .slide {
        padding: 10px;
    }
}

@media (max-width: 768px) {
    .slick-prev, .slick-next {
        left: 10px; /* Resets the previous button to a closer edge */
        right: 10px; /* Resets the next button to a closer edge */
    }

    .recently-viewed-slider .slide {
        margin: 0 5px; /* Smaller margin for mobile */
    }
}


/* Image Styling */
.recently-viewed-slider .slide img {
    max-width: 100%; /* Ensures image does not exceed the card width */
    height: auto;
    margin-bottom: 15px; /* Space between the image and the text */
}

/* Heading inside Slide */
.recently-viewed-slider .slide h3 {
    color: #333; /* Darker text for better readability */
    margin-bottom: auto; /* Pushes everything else down */
    font-size: 16px; /* Adjusts the font size */
    line-height: 1.4; /* Line height for better readability */
    max-height: 2.8em; /* Based on font-size and line-height to fit exactly two lines */
    overflow: hidden; /* Hides overflow text */
    text-overflow: ellipsis; /* Adds an ellipsis to overflow text */
    display: -webkit-box;
    -webkit-line-clamp: 2; /* Limit text to two lines */
    -webkit-box-orient: vertical; /* Required for line clamping */
    margin-top: 0;
}


/* Link Styling */
.recently-viewed-slider .slide a.more-link {
    display: inline-block; /* Makes the link a block for margins */
    text-decoration: none; /* Removes underline from links */
    color: #DE4F3E; /* Link color */
    background-color: #f8f8f8; /* Subtle background for the link */
    border-radius: 5px; /* Rounded edges for the link */
    transition: background-color 0.3s, color 0.3s; /* Smooth transition for hover effects */
     margin-top: auto; /* Aligns button at the bottom */
    padding: 8px 16px; /* Larger clickable area */
}

.recently-viewed-slider .slide a.more-link:hover {
    background-color: #DE4F3E; /* Background color on hover */
    color: white; /* Text color on hover */
}

/* Slick Arrow Button Styling */
/* General styles for navigation arrows */
.slick-prev, .slick-next {
    color: #333; /* Arrow color */
    background-color: transparent; /* Background color */
    border: 1px solid #DE4F3E; /* Border for the buttons */
    border-radius: 50%; /* Circular button */
    width: 36px; /* Width of the buttons */
    height: 36px; /* Height of the buttons */
    line-height: 36px; /* Center the icon vertically */
    text-align: center; /* Center the icon horizontally */
    opacity: 0.75; /* Slightly transparent for a lighter look */
    transition: opacity 0.3s; /* Smooth fade for hover effect */
    position: absolute; /* Absolute position relative to the slider container */
    top: 50%; /* Align vertically at the middle */
        transform: translateY(-50%) translateX(-50%); /* Center vertically and horizontally */

    z-index: 25; /* Ensure it's above all other content */
    align-items: center;
}

.slick-prev {
    left: -50px; /* Move the previous button to the left of the container */
}

.slick-next {
    right: -50px; /* Move the next button to the right of the container */
}
.slick-prev:hover, .slick-next:hover {
    background-color: #DE4F3E; /* Change background color on hover */
    opacity: 1; /* Make fully opaque on hover */
}


@media (max-width: 768px) {
    .slick-prev, .slick-next {
        display: none; /* Hide arrows on mobile */
    }
}







